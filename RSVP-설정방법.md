# RSVP(참석 의사) 저장 기능 설정 방법

청첩장 자체는 정적 HTML이라 데이터를 저장할 수 없어요. 그래서 **Google 시트를
데이터 저장소로, Apps Script를 그 앞단 서버로** 사용합니다. 참석자 명단은 별도
관리자 페이지 없이, 응답이 쌓이는 **Google 시트를 직접 열어서** 확인하시면 됩니다.
이 작업은 구글 로그인이 필요해서 제가 대신 해드릴 수 없고, 아래 순서대로 직접
진행해주시면 됩니다. 10분 정도 걸려요.

## 1. 새 구글 시트 만들기
1. [sheets.new](https://sheets.new) 접속 (구글 계정 로그인 상태여야 함)
2. 제목을 "김유빈 박지인 RSVP" 등으로 변경

## 2. Apps Script 붙여넣기
1. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭
2. 기본으로 열려 있는 `Code.gs`의 내용을 전부 지우고,
   이 프로젝트의 `js/rsvp-apps-script.gs` 파일 내용을 통째로 복사해서 붙여넣기
3. 저장 (Ctrl+S / 디스크 아이콘)

## 3. 웹앱으로 배포
1. 우측 상단 **배포 → 새 배포** 클릭
2. 유형 선택(⚙️ 톱니바퀴)에서 **웹 앱** 선택
3. 설정:
   - 설명: 아무거나 (예: rsvp v1)
   - 다음 사용자 인증 정보로 실행: **나**
   - 액세스 권한이 있는 사용자: **모든 사용자**
4. **배포** 클릭 → 처음엔 구글이 권한 승인을 요구합니다.
   "고급" → "(안전하지 않음) OO(으)로 이동" → 허용 순서로 진행
5. 배포가 끝나면 **웹 앱 URL**이 나옵니다 (`https://script.google.com/macros/s/…/exec` 형태).
   이 URL을 복사해두세요.

## 4. 사이트에 URL 연결
`js/main.js` 파일을 열어서 아래 줄을 찾아 URL을 붙여넣으세요.

```js
var APPS_SCRIPT_URL = "여기에 3번에서 복사한 웹앱 URL 붙여넣기";
```

## 5. 참석 응답 확인하기
하객이 청첩장에서 "참석 의사 전달하기"를 누르면, 1번에서 만든 Google 시트의
`RSVP` 탭에 실시간으로 한 줄씩 쌓입니다. 그냥 그 시트를 열어보시면 됩니다.
(이름 / 구분(신랑측·신부측) / 참석여부 / 인원수 / 연락처 순으로 기록됩니다.)

## 6. 코드를 고칠 때마다
Apps Script 코드 자체를 나중에 수정하면, **배포 → 배포 관리 → 연필 아이콘(수정)
→ 새 버전 → 배포** 를 다시 눌러야 실제 웹앱에 반영됩니다. (URL은 바뀌지 않아요.)
